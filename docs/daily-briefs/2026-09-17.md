# PM Brief — 2026-09-17

## Yesterday's progress

**main — 6 substantive commits:**

- **M1 P1 repaired:** `cb805a8` / `e375f64` — B2 lane now owns post-assessment turns for the founder cohort before legacy routing applies. Directly closes the P1 flagged in the 09-16 brief.
- **Trust boundary hardened:** `c06ce16` / `088409a` — three-layer trust boundary, deterministic B2-aligned validator, no publishing holes in new-system lane.
- **Fallback authority invariant landed:** `e7988a6` / `9ac11b8` — explicit new-system isolation; fallback no longer bleeds into B2-owned turns.

Yesterday's P1 triage task landed cleanly. A8 disposition and A7 commit did not land.

**Drift signal:** Six commits in 24 hours on M1 post-ship repair is appropriate — live P1 warranted it. But the structural gates (A7, A8 closure) remain unstarted after the M1 work settled. The P1 that was fixed has no entry in `docs/issues-log.md`.

---

## Today's focus

1. **Log the M1 P1 in `docs/issues-log.md`:** B2 lane authority failure over founder post-assessment turns, fixed in `cb805a8`. ~10 minutes.
2. **A8 disposition — make the call:** `apps/web/src/qa/harness/` has existed for weeks; `behavioral-regression.test.ts` has not. Decide whether the harness satisfies A8 or the file still needs to be created. A written decision, not more deferral. This is the sixteenth consecutive brief on this item.
3. **A7 — commit ConfidenceLevel:** `type ConfidenceLevel = 'high' | 'medium' | 'low' | 'insufficient'` in `apps/web/src/lib/confidence-semantics.ts` with one passing assertion. ~30 minutes, fully independent of vNext.

---

## Why this is the priority

M1 is now stable with its trust boundary corrected and the fallback authority invariant enforced. The B2 lane is live for real users, which raises the cost of a future behavioral regression — making A7 and A8 more urgent, not less. A7 is a hard gate before B3 (engine extraction) and takes under an hour. A8's sixteen-brief deferral ends with a decision today, not a seventeenth recommendation.

---

## Blockers / open issues to triage first

- M1 P1 (B2 lane authority over post-assessment turns, fixed in `cb805a8`) is not logged in `docs/issues-log.md`. Log it before closing the loop.

---

## Scope-creep risk

Five quality-correction commits just landed on M1 — the pull toward refining the trust boundary further (new-system edge cases, more vNext evaluation arms) is real and should be resisted until A7 and A8 are committed.
