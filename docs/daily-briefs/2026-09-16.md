# PM Brief — 2026-09-16

## Yesterday's progress

**main — 6 substantive commits since the 09-15 brief:**

- **QA harness completed:** `6877aaa` — full evaluation infrastructure at `apps/web/src/qa/harness/` (corpus, invariants, mutations, judge, CLI, schema). This is a materially larger behavioral QA system than the `behavioral-regression.test.ts` file A8 called for.
- **QA harness WIP halted by §20 stop rule:** `0c1084b` — a live P1 was found during harness build. The harness was subsequently completed; P1 disposition is unknown — not logged in `docs/issues-log.md`.
- **vNext Phase 0 ran and judged:** `2dd68ee` — three-arm architecture experiment (B1/B2 arms) completed and decided.
- **vNext Phase 0.1 ran and decided:** `7a6952f` — B1/B2 substrate conditions validated.
- **Migration 1 shipped:** `73bab2a`, `a1f1121` — the validated B2 reasoning lane is now live for the founder cohort. This is the largest architectural change since the engine-vs-domain boundary.

**From the 09-15 focus — NOT landed:**
- `apps/web/src/lib/__tests__/behavioral-regression.test.ts` — **Fifteenth consecutive brief.** However, `apps/web/src/qa/harness/` is now a more capable system serving the same intent. A decision about A8's disposition is overdue.
- `apps/web/src/lib/confidence-semantics.ts` (A7) — not created.

**Drift signal:** The work that landed is legitimate and high-value — the vNext experiment is directly in line with improving advisory quality. The pattern remains: architectural advances ship; structural gates (A7, A8 closure) do not. The P1 flagged and then apparently resolved without a log entry is worth a second look.

---

## Today's focus

1. **Triage the P1 from `0c1084b`:** Check what was found, confirm whether it was fixed in a subsequent commit, and add an entry to `docs/issues-log.md` if it was not already logged. ~15 minutes.
2. **Close or redirect A8:** Either confirm `apps/web/src/qa/harness/` satisfies A8's intent and note the decision in a brief comment in `docs/implementation-plan.md`-adjacent context — or create the minimal `behavioral-regression.test.ts` file as originally scoped. A decision, not more deferral.
3. **A7 — commit ConfidenceLevel:** `type ConfidenceLevel = 'high' | 'medium' | 'low' | 'insufficient'` in `apps/web/src/lib/confidence-semantics.ts` with one passing assertion. Separate commit.

---

## Why this is the priority

Migration 1 is live; the B2 lane is now serving real users. That raises the cost of a behavioral regression — which is precisely what A8 and A7 exist to prevent. The new QA harness gives a strong foundation, but A7 (confidence semantics) is a hard gate before B3 (engine extraction) and is fully independent of vNext work. The P1 triage is a 15-minute hygiene check that closes an unknown-open loop.

---

## Blockers / open issues to triage first

- **Unknown P1 from `0c1084b`:** found during harness WIP, disposition unclear, not in issues-log. This is the one item to check before anything else today.

---

## Scope-creep risk

Migration 1 just shipped clean — the temptation is to extend the vNext evaluation framework (Phase 1, more arms) rather than stopping to document what was decided and close the structural gates.
