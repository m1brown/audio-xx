# PM Brief — 2026-09-08

## Yesterday's progress

**main branch — 2 commits (landed after the 09-07 brief):**
- `4110cc8` — `fix: homepage clarity — the proposition, the job, and what to ask` (`apps/web/src/app/page.tsx`)
- `dc0af56` — `release: homepage clarity patch — proposition-led hero with concrete capability copy` (`apps/web/src/app/page.tsx`, `public-beta-copy-and-affiliate-discipline.test.ts`)

**What did not land from the prior focus:** A8 (behavioral regression harness — one assertion committed) and the stall diagnosis proposed on 09-07. This is now the **eighth consecutive brief** in which A8 is the stated focus and zero commits toward it have landed.

**Drift flag:** Both commits are homepage copy and copy-discipline test passes — off the stated priority. The work is not harmful (updating `public-beta-copy-and-affiliate-discipline.test.ts` alongside a homepage change shows appropriate hygiene), but the pattern is now unambiguous: lower-resistance copy and UI work is consistently displacing the test infrastructure task. This is avoidance, not a tooling gap or an underspecified task.

---

## Today's focus

1. **A8 — one routing assertion, committed:** Open `apps/web/src/lib/__tests__/behavioral-regression.test.ts`. Write exactly one assertion: given `"best DAC under $1500"`, the routing lands in the expected shopping `ConversationMode`. The test may stub the LLM call or assert directly on routing logic. Commit. The file existing with one green (or explicitly-pending) assertion breaks the eight-session pattern.

2. **A7 — type definition + one test:** Add `'high' | 'medium' | 'low' | 'insufficient'` as an explicit union type in `apps/web/src/lib/` (new `confidence-semantics.ts` or inline in `inference-layer.ts`). One line of type, one test that would fail if a string outside the union were used. Commit separately.

---

## Why this is the priority

A8 gates every A6 misalignment fix. A6 gates B3. B3 gates Climate Screen engine consumption. The journalist-outreach target was late May 2026 — it is now September 2026. The homepage is complete enough to share with friends today. Further UI iteration before A8 lands is not progress; it is the same stall in a different disguise.

---

## Blockers / open issues to triage first

None in the issues log (seed entry only). The practical blocker is still unnamed — identifying it is thirty minutes of honest reflection that unlocks the rest of the plan.

---

## Scope-creep risk

The homepage copy just landed cleanly; the pull today will be toward "one more clarity pass" on copy or the intake flow. The page is good enough to send to friends. Don't let it become the default safe harbor again.
