# PM Brief — 2026-09-03

## Yesterday's progress

Three commits landed on `main`, all touching affiliate disclosure:

- `454a581 fix(disclosure): the Affiliate Disclosure page described a state that is no longer true`
- `e9e1040 fix(disclosure): every public affiliate statement derives from the configuration`
- `ab6381c merge: affiliate disclosure derives from configuration (production tracks main)`

**What did not land from the prior focus:** A8 (behavioral regression harness) and A7 (confidence semantics lock-in) — now five consecutive sessions without either file appearing.

**Drift flag:** Affiliate disclosure fixes are legitimate pre-beta compliance work, but they extend an already-long run of off-plan activity. The affiliate disclosure arc is now closed. There is no comparable pull remaining before A8.

---

## Today's focus

1. **A8 — behavioral regression harness:** create `apps/web/src/lib/__tests__/behavioral-regression.test.ts` with the eight canonical prompts and behavioral property assertions (routing, confidence language, trade-off presence, continuity, restraint, structural fields). One file, one commit.

2. **A7 — confidence semantics lock-in:** define `'high' | 'medium' | 'low' | 'insufficient'` with explicit output-language contracts in `apps/web/src/lib/__tests__/confidence-semantics.test.ts`.

---

## Why this is the priority

The first-25 beta is live or imminent. Behavioral misalignment fixes in response to real-user feedback have no anchor without A8 — every change risks making the system "just different" rather than measurably better. A7 must still precede B3 extraction, which gates Climate Screen engine consumption. The affiliate disclosure work is done; there is no remaining queue of legitimate interrupts ahead of these two files.

---

## Blockers / open issues to triage first

None. Issues log contains only the seed entry. No open issues from prior briefs.

---

## Scope-creep risk

With the disclosure arc closed, the new pull is likely toward beta-facing UI polish or launch-prep tasks rather than the behavioral test files. Write one failing assertion in `behavioral-regression.test.ts` before opening any UI file.
