# Production Promotion Report — M2 Baseline

**Date:** 2026-07-22 · **Task:** Promote approved `version-b` HEAD to production, verify, stop.

## Release

| | |
|---|---|
| Previous production commit | 17 July deployment (`audio-xx-s6losmxgd`, CLI deploy — no git sha recorded in Vercel metadata) |
| Deployed commit | **`d8ea67b`** — confirmed two ways: local HEAD == `origin/version-b` == `d8ea67b` at deploy, and the live snapshot provenance line renders "engine d8ea67b" (baked from `VERCEL_GIT_COMMIT_SHA` at build) |
| Deployment | `audio-xx-elrmk9iuv` · built and Ready 22 July 2026 ~13:55 CEST · Vercel project `audio-xx-web` |
| Production URL | `https://audio-xx.com` (+ `www`) — both aliases confirmed pointing at the new deployment |
| Database target | Turso libsql, `aws-eu-west-1.turso.io` (via `TURSO_DATABASE_URL`/`TURSO_AUTH_TOKEN`; no secrets printed) |

Working tree was clean at deploy (one untracked local `.vercel`-ignore file, not part of the release). No billing/Stripe dependency exists in the release. No local-only files: the catalog index is committed; `prisma generate` runs in the Vercel build.

**Environment variables (existence/scope only):** `DATABASE_URL`, `TURSO_DATABASE_URL`, `TURSO_AUTH_TOKEN`, `NEXTAUTH_SECRET`, `NEXTAUTH_URL`, `OPENAI_API_KEY`, `NEXT_PUBLIC_ASSESSMENT_ARTIFACT_V2` — all present and production-scoped.

## Database

- **Backup:** full recoverable dump taken before any change — complete schema DDL + every row of every table, as both replayable SQL and JSON (kept off-repo; contains user data). Recovery = replay schema + data dump into a fresh database. Pre-change contents: 1 user, 1 profile, 3 systems, 8 system_components, 8 catalog components.
- **Exact schema change** (generated by `prisma migrate diff` against a byte-accurate local mirror of the real production schema, reviewed before applying):
  1. `ALTER TABLE "systems" ADD COLUMN "canonical_text" TEXT` (nullable)
  2. `CREATE TABLE "assessment_snapshots" (…)` with FK → `systems` ON DELETE CASCADE
  3. `CREATE INDEX "assessment_snapshots_system_id_idx"`
  Confirmed: **no** dropped tables/columns, no type changes, no data operations, no table rebuilds, no row modifications.
- **Verification after apply:** table exists ✓ · column exists and nullable ✓ · index exists ✓ · FK cascade correct ✓ · every table's row count identical to backup ✓ · all 3 existing systems readable, `canonical_text` NULL as expected ✓ · user row field-identical to backup ✓.

## Automated tests (at `d8ea67b`)

| Gate | Result |
|---|---|
| Product suite | **29/29 passing** |
| Engine regression gate | **3,793 passing · 20 pre-existing baselined failures · 0 new** |
| Build + type-check | Production Vercel build (`prisma generate && next build`) succeeded |

No new failures; the 20 baselined failures are the long-standing trusted-baseline entries, unchanged.

## Live acceptance on audio-xx.com

| Check | Result |
|---|---|
| System Builder (typeahead "bifrost" → *Schiit Bifrost 2/64 — DAC*, click-to-fill, free text) | ✅ |
| Assessment (builder → server-rendered artifact with verdict) | ✅ |
| Sharing (Copy link → "Link copied"; URL self-contained) | ✅ |
| Printing (`?print=1` chrome-free, verdict intact) | ✅ |
| Malformed input → guided "Build your system →" path | ✅ |
| Save flow (anonymous → `/save` "Begin your collection" → account created → saved → `/systems?welcome=1`) | ✅ |
| Authentication (sign-out clears session; API 401s signed-out; sign-in restores) | ✅ |
| My Systems (card with chain, verdict pull-quote, date) | ✅ |
| Saved snapshot (immutable payload; provenance "Prod Gate Test Rig · saved 22 July 2026 · engine d8ea67b"; notes; Run today's assessment link) | ✅ |
| Rename + notes (persist, survive re-save) | ✅ |
| Duplicate protection (re-save → `duplicate: true`, 1 system, snapshot count 1→2, name/notes untouched) | ✅ |
| Per-user isolation (test account sees 1 system; pre-existing user's 3 systems invisible to it) | ✅ |
| Conversational composer + engine routes | ✅ — budget tube-amp query: **max $2,800, no Shindo** (shopping guardrail); "Can my Nait 50 drive LS3/5a?" → **knowledge lane, no price cards**; assessment → "What should I upgrade first?" → **focused answer, no re-ask** (follow-up continuity); assessment consistency held (bottleneck verdict + coherent recommendation) |
| Mobile 375 px (landing/builder, My Systems, saved snapshot) | ✅ no horizontal overflow anywhere; CTA 55 px tall; builder inputs 35 px (usable; slightly under the 44 px ideal — polish note) |

**Test data:** used clearly-identified `prod-gate-test@audio-xx.com` / "Prod Gate Test Rig". The test system and both its snapshots were deleted after testing (DB verified back to 3 systems / 0 snapshots). The test **account row remains** — there is no account-deletion endpoint; it is inert and clearly named. No real user data was touched.

## Issues

**Deployment-config fix applied (within fix policy):** production `NEXTAUTH_URL` pointed at a stale preview domain (`audio-xx-web-git-main-…vercel.app`), not `https://audio-xx.com`. Corrected before deploy. Side effect: the old value was one record spanning Dev/Preview scopes and was removed with it; previews now fall back to NextAuth's `VERCEL_URL` handling, which is more correct than the stale value. Auth verified working on the production domain post-fix. (Configuration change only — nothing to commit.)

**Known limitations (not release-blocking):**
- Schiit Bifrost 2/64's product photo renders as an empty frame in the artifact photo strip on production (external image fails to load; the null-skip logic covers *missing* URLs, not *broken* ones). Cosmetic; M5 polish list.
- Builder touch targets at 35 px vs the 44 px ideal (M3/M5 polish).
- Vercel warns the project's Node 20 build image is deprecated after 2026-10-01 — set `"engines": {"node": "24.x"}` before then (operations note, not this release).
- The pre-existing production user's 3 systems predate M2 (no snapshots) — they render in My Systems without verdict/Read-assessment, as designed.

**Founder-only manual check remaining:** none required — the full signed-in journey was verified with the test account on the production domain. If you want belt-and-braces, a 60-second walk-through of build → save → My Systems with your own account is the only thing I couldn't do as *you*.

## Recommendation

**Production baseline approved — ready to begin Milestone 3.**
